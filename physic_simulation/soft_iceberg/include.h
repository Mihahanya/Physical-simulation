#pragma once

#include <vector>
#include <tuple>
#include <optional>
#include <SFML/Graphics.hpp>
#include <iostream>

using std::cout;
using std::min;
using std::max;
using std::vector;
using std::optional;
using std::tuple;
using std::tie;
using std::get;
using std::nullopt;

using sf::Vector2f;
using sf::Vertex;
using sf::Color;
using sf::RenderWindow;
using sf::Keyboard;
using sf::Mouse;
using sf::Event;
using sf::LinesStrip;
using sf::Lines;
using sf::Style::Close;
using sf::Style::Titlebar;
